/**
 * Returns true if the number is lower than or equal to max, otherwise false.
 * @param number - {Number}
 * @param max - {Number}
 * @returns {boolean}
 */
export const maxValue = (number: number, max : number) : boolean => {
    return number <= max;
  }