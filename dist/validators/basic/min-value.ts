/**
 * Returns true if the number is higher than or equal to min, otherwise false.
 * @param number - {Number}
 * @param min - {Number}
 * @returns {boolean}
 */
export const minValue = (number: number, min : number) : boolean => {
    return number >= min;
  }